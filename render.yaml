services:
  - type: web
    name: threattrace-backend
    env: python
    rootDir: threat-trace-backend
    buildCommand: pip install -r requirements.txt
    startCommand: python run.py
    plan: free
